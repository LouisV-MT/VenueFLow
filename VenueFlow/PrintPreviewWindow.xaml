<Window x:Class="VenueFlow.PrintPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Print Preview" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Document Preview" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,10"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Background="White">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="None"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Click="BtnCancel_Click" Style="{StaticResource MaterialDesignOutlinedButton}" Content="Cancel" Margin="0,0,10,0"/>

            <Button Click="BtnPrint_Click" Style="{StaticResource MaterialDesignRaisedButton}" ToolTip="Send to Printer or PDF">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Printer" Margin="0,0,8,0"/>
                    <TextBlock Text="PRINT / SAVE"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Window>